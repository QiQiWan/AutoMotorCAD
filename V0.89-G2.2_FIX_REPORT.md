# V0.89-G2.2 Fix Report

## Evidence from the 2026-08-27 diagnostic export

- `MODEL_RUNTIME_CHECK` at `2026-08-27T08:32:57.648036+00:00` reports `PASS` for `i5_Industrial_SPM_Servo_Tooth_Wound`.
- The next line records `MODEL_RUNTIME_CHECK_JOIN_RESULT`, confirming the in-flight single-flight repair is working.
- The screenshot label `Motor-CAD 计算检查 / 未通过 / 提交前需运行完整计算前检查` therefore represented an **unexecuted/pending gate**, not a failed native Motor-CAD check.
- Four validation analyses were subsequently submitted around 08:32:59–08:33:04. With Worker/EMag capacity 1, three cases queued and the monitor raised `LICENSE_EMAG_BUSY, WORKER_CAPACITY`. This explains residual UI latency during standard validation execution.
- The remaining frontend root cause in the export was `FRONTEND_UNHANDLED_REJECTION: 分析必须绑定已验证的电机版本。` during route reconciliation. This is fixed by treating a deep/stale analysis id as a hint until motor-revision ancestry exists.

## Root fixes

1. **Shell authority**: desktop project shell is now a 52 px flex row. Project identity and the 3 stages have bounded widths; stage buttons no longer stretch across most of the viewport; status/next action consume only the remaining right-side space.
2. **Design/Analysis ownership**: stator, rotor, magnet, conductor and housing materials are owned only by Design Revision. The legacy Analysis `materials` input domain is hidden and ignored when materializing solver inputs.
3. **Coolant ownership**: coolant/flow remains an Analysis boundary condition. It no longer creates a false `Design Revision material override` warning.
4. **Motor-CAD gate truth**: the native calculation card is tri-state. Before a check it displays `待检查`; only a real failed native check displays `未通过`.
5. **Issue deduplication**: repeated Studio/Task warnings are deduplicated before rendering.
6. **Route hardening**: stale/deep-link analysis ids cannot throw an unhandled context rejection on overview/project navigation.

## Verification

- Python compile: PASS
- JavaScript `node --check`: PASS
- Targeted regression: `32 passed, 209 deselected`

The current environment cannot execute the user's Windows Motor-CAD 2026R1 instance. Native workstation acceptance should confirm that the calculation card moves `待检查 -> 已通过` after the full check, and that only one Motor-CAD feasibility check is launched for the same model fingerprint.
