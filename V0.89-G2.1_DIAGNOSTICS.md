# MotorCAD Studio V0.89-G2.1 — UI、交互性能与 Motor-CAD 诊断收敛

日期：2026-08-27

## 1. 结论摘要

本轮排查确认了三条相互耦合的故障链：

1. 顶部工程导航同时叠加“项目上下文 + 设计/验证/决策 + 当前/状态/下一步 + 当前工程步骤”，并且 `styles.css` 与 `global-shell-convergence.css` 中存在多层重复覆盖，造成顶部区域高、宽、层级和视觉重心不稳定。
2. “按钮点不动”主要由重复提交与单机 Motor-CAD 串行容量冲突触发。日志中同一 `standard-validation-package/execute` 在同一轮出现 4 个并发 POST，分别耗时约 182–204 s；同时分析配置路由曾达到 400102 ms。前端按钮在异步请求期间被重新渲染成新节点，失去 disabled 状态；后端完成态缓存又不能合并正在运行的相同检查，导致一次点击链被放大为多个 Motor-CAD 实例/检查。
3. 最新模型检查的实际阻断项集中在模板继承材料的原生回读语义：`Conductor` 与 `Housing` 的 PyMotorCAD component display-name 候选未命中。参数回读、绕组、几何均已通过。模板 MTT 已明确携带 `Copper (Annealed)` 和 `Aluminium (Alloy 195 Cast)`，所以这次失败属于“原生组件别名证据缺口”被设计时检查当成材料错误。

本轮源码修订为 V0.89-G2.1，围绕上述三条链做了 UI Authority、请求 Single-flight、幂等提交、交互扫描降载、分析配置缓存、Motor-CAD I/O 证据输出，以及模板继承材料检查语义收敛。

## 2. 顶部导航修复

### 原问题

页面同时存在：

- 全局导航：项目 / 运行环境 / 问题 / 高级工具；
- 项目身份：Current project；
- 工程阶段：设计 / 验证 / 决策；
- Focus Bar：当前 / 状态 / 下一步；
- Engineer Journey Cue：当前工程步骤 + CTA；
- 页面内部仍有“验证 / 检查并计算”。

这导致同一语义至少出现两到三次。CSS 侧还有 `styles.css`、G1、G1R、G2 多层规则共同管理 `.project-shell`、`.engineer-focus-bar-v089f`，布局权威不唯一。

### G2.1 修复

在 `static/global-shell-convergence.css` 增加最终 Authority：

- >=1181 px：项目身份、阶段导航、状态/下一步收敛到同一 56 px 行；
- 默认隐藏 Focus Bar 的“当前”列，避免再次复述页面标题；
- Engineering/Operator 模式隐藏 Engineer Journey Cue，避免再出现一层“当前工程步骤”；
- 821–1180 px 采用紧凑双行；
- 移动端继续保留堆叠式回退；
- 只保留一个显著下一步 CTA。

修改文件：

- `motorcad_studio/static/global-shell-convergence.css`
- `motorcad_studio/static/index.html`（变更静态资源版本戳）

## 3. 按钮响应差的日志证据与修复

### 日志证据

`raw/studio.jsonl` 记录：

- `/simulation/analyses`：61943 ms；
- `/simulation/analyses/{analysis}/configure/definition`：87597 ms；
- `/simulation/analyses/{analysis}`：400102 ms；
- 同一个 `standard-validation-package/execute` 在同一时刻完成 4 个并发请求：
  - 190459 ms
  - 182384 ms
  - 199204 ms
  - 203516 ms
- 后续同一接口仍连续出现约 59–74 s 请求。

截图中的 toast `×4` 与四重并发提交完全吻合。

### 根因

前端 `standard-validation.js` 原实现只禁用“当前 DOM 按钮”。异步请求期间 `render()` 会替换容器 `innerHTML`，生成一颗新的、可点击按钮，造成重复执行。后端 `_model_runtime_check_cache` 只复用已经结束的结果；相同 fingerprint 在首个检查尚未结束时仍会各自启动 Motor-CAD 资格检查。当前 runtime contract 的 Worker/License 容量为 1，这会形成资源排队并拖慢 AnyIO/API 路由。

`action-readiness.js` 还存在一次刷新扫描两次 DOM，以及 `input/change` 每次同步全页扫描的问题；全局 MutationObserver 会进一步放大重绘成本。分析配置初始化也重复获取 catalog/template catalog，并反复生成相同 preview。

### G2.1 修复

- 前端 Standard Validation 增加 `state.running`、`runPromise`、`runKey`、`submissionKey`；重复点击直接复用正在运行的 Promise；重绘后按钮持续显示“检查中…”并保持 disabled。
- 后端为相同模型 fingerprint 增加 in-flight single-flight；跟随请求等待 leader 并复用刚完成的 cache，不再重复启动 Motor-CAD。
- Standard Validation 为子分析任务生成稳定 `submission_key`，衔接已有任务幂等逻辑。
- 几何检查 work directory 改为 `毫秒时间戳 + UUID`，避免并发证据目录碰撞。
- `action-readiness.js`：一次扫描复用结果、40 ms 有界 debounce、忽略 helper-only MutationObserver 变更、运行时返回 `BUSY`。
- `unified-configuration.js`：按 revision 缓存 catalog/template catalog，并缓存最近 24 个 template preview。

修改文件：

- `static/analysis/standard-validation.js`
- `static/workflow/action-readiness.js`
- `static/analysis/unified-configuration.js`
- `models.py`
- `main.py`

## 4. 当前“Motor-CAD 模型检查未通过”的真实原因

最新诊断中：

- RPC：PASS
- Template load：PASS
- Parameter read：PASS（26/26）
- Parameter roundtrip：PASS
- Winding：PASS
- Geometry：PASS
- Materials：FAIL，2 项：`Conductor`、`Housing`
- NativeModelSnapshot：PARTIAL，根故障 `NATIVE_MATERIAL_READBACK_UNRESOLVED`

材料审计显示：

- Stator Lamination -> `Stator Lam (Back Iron)` / `Stator Lam (Tooth)`，回读 `M350-50A`，成功；
- Rotor Lamination -> `Rotor Lam (Back Iron)`，回读 `M350-50A`，成功；
- Magnet -> `Magnet`，回读 `N30UH`，成功；
- Conductor 的候选 `Conductor`、`Copper - Active`、`Copper (Active)`、`Winding Conductor`、`Stator Winding` 均未命中；
- Housing 的候选 `Housing`、`Housing (Active)` 均未命中。

模板 `i5_Industrial_SPM_Servo_Tooth_Wound.mtt` 中已经存在：

- `Material_Housing_Active=Aluminium (Alloy 195 Cast)`
- `Material_Housing_[A]=Aluminium (Alloy 195 Cast)`
- `Material_Copper_Active=Copper (Annealed)`
- `Material_Copper_-_Active=Copper (Annealed)`

因此当前证据支持的根因是：V0.88-A material semantic profile 对 Motor-CAD 2026R1 当前模板的精确 Component display name 尚未资格化，模板继承材料却被作为 required native readback 阻断。

### G2.1 修复语义

`native/motorcad/planner.py` 将材料读取策略拆成两类：

- 用户显式赋值 / 非模板继承材料：仍要求严格 write + readback，失败直接阻断；
- 模板继承材料且 semantic profile 尚未资格化：仍执行 best-effort 原生回读，但记录为 `best_effort_inherited` observability gap，不再在“设计时模型可行性检查”中当成材料本体错误；一旦 profile 完成资格化，自动恢复严格 required readback。

Production Native Closure 的独立 required-material contract 未降低，所以这次调整不会让生产资格检查虚假通过。

修改文件：

- `native/motorcad/planner.py`
- `solvers/motorcad.py`

## 5. 新增 Motor-CAD 输入/输出日志

每次模型运行时检查现在会在唯一 `work_dir` 中输出：

- `motorcad_input.json`：模板、模板路径/来源、analysis、显式参数、effective parameters、材料、repair policy、目标版本；
- `motorcad_io.jsonl`：session、model load、binding plan、逐项 executor event、参数 write/readback audit、材料 readback audit、消息阶段、最终 root cause；
- `motorcad_output.json`：最终 qualification payload、checks、Motor-CAD messages、installation、root cause、NativeModelSnapshot、fault tree、repair plan；
- `motorcad_messages_post_binding.json`：binding/readback 阶段的原始 Motor-CAD messages；
- 既有 `motorcad_binding_plan.json`、`model_validation.json`、winding artifacts 等继续保留。

API `/geometry-check` 的响应和全局 `MODEL_RUNTIME_CHECK` 审计也增加 `motorcad_io_artifacts`，后续从 Studio 日志就能直接定位本次检查的 I/O 证据目录。

## 6. 两条次要诊断信息

### `pymotorcad: Coil index too high`

当前 Native Winding Readback 会逐 coil 读取，直到探测到超出范围，以此识别每条 path 的实际 coil 数。这条消息可能由边界探测产生。最新日志中 winding 与 geometry 均为 PASS，所以它没有构成这次模型失败的根因。

G2.1 会先保存 binding 阶段原始消息，再 `clear_message_log()`，让后续 native validation 使用独立消息阶段，避免边界探测消息污染根因诊断。

### `Unable to save ... Defaults.INI`

日志出现：Motor-CAD 无法写入 `%APPDATA%\Ansys\v261\MotorCAD\Defaults.INI`。在此前四重并发的情况下，文件竞争本身就可能触发这类警告；也可能存在目录/ACL 问题。当前证据还不足以单独确定是哪一种。

G2.1 将其单独标记成 `motorcad_user_profile_write: WARN`。实机复测建议先确认只存在一条模型检查；若单实例仍复现，再检查 `C:\Users\EatRice\AppData\Roaming\Ansys\v261\MotorCAD` 的目录存在性和写权限，并让 Motor-CAD 交互式启动一次完成用户配置初始化。

## 7. 实机复测验收标准

建议在 Windows + Motor-CAD 2026R1 上执行一次全新 G2.1 启动后测试：

1. 浏览器强制刷新，确认加载 `?v=0.89.9-g2.1` 静态资源。
2. 进入“验证 → 分析配置”，顶部项目 shell 在桌面宽度下保持单行，默认不再出现重复“当前工程步骤”。
3. 连续快速点击“检查并计算”3–5 次；前端始终只显示一个“检查中…”，服务端同 fingerprint 应出现一个 `MODEL_RUNTIME_CHECK` leader，其余只记录 `MODEL_RUNTIME_CHECK_JOINED/JOIN_RESULT`。
4. 本次 work_dir 应出现 `motorcad_input.json`、`motorcad_io.jsonl`、`motorcad_output.json`、`motorcad_messages_post_binding.json`。
5. 对 i5 模板默认/继承材料，`materials.failed` 应为空；Conductor/Housing 若精确 Component alias 尚未资格化，应进入 `observability_gaps`。
6. 参数、绕组、几何通过后，不应再因模板继承材料别名缺口弹出“模型检查未通过”。
7. 若仍出现 `Defaults.INI` 写入 WARN，在单实例状态下再处理 AppData ACL。

## 8. 验证状态

已完成：

- Python 变更文件 `py_compile` 通过；
- 3 个变更 JavaScript 文件 `node --check` 通过；
- V0.88-A/B/C、V0.89-G2、Standard Validation/HMI 定向回归：52 passed，2 deselected；
- Action Readiness 定向静态回归：9 passed。

全量 `pytest -q tests` 在当前沙箱 180 s 测试窗口内未跑完，已超时终止，因此不将其表述为“全量通过”。最终生产结论仍需要 Windows + Motor-CAD 2026R1 实机回归确认。

## 9. 本轮源代码修改清单

- `motorcad_studio/main.py`
- `motorcad_studio/models.py`
- `motorcad_studio/native/motorcad/planner.py`
- `motorcad_studio/solvers/motorcad.py`
- `motorcad_studio/static/analysis/standard-validation.js`
- `motorcad_studio/static/analysis/unified-configuration.js`
- `motorcad_studio/static/global-shell-convergence.css`
- `motorcad_studio/static/index.html`
- `motorcad_studio/static/workflow/action-readiness.js`
